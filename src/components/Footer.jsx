import React, {useState, useEffect} from 'react'

export default function Footer() {
  return (
    <div>Footer</div>
  )
}

//Crear un componente: Se crea el archivo con Mayúscula, .js(x) y luego una funcion que se llame igual que
// el archivo. Luego a la función le ponemos "export default"


